---
specVersion: 1
id: 2026-09-18-002
scope: "Nozzle 2: Jev-routed tool/namespace surfacing for Pi"
status: pending
milestones:
  - id: M1
    name: "Namespace surfacing core"
    status: verified
    cwd: /data/projects/jev-context
    architecture: single
    verifyCommand: npm run check && npm test
    visualVerify: none
    prerequisites: []
    capabilityScope:
      - extensions/jev-context.ts
      - extensions/jev-context.test.ts
    contextRefs:
      - VERIFYING.md
  - id: M2
    name: "Degradation + escape hatch"
    status: pending
    cwd: /data/projects/jev-context
    architecture: single
    verifyCommand: npm run check && npm test
    visualVerify: none
    prerequisites: [M1]
    capabilityScope:
      - extensions/jev-context.ts
      - extensions/jev-context.test.ts
    contextRefs:
      - VERIFYING.md
---

# Nozzle 2: Tool surfacing

Execute directly. Do NOT delegate to heavy_think. Binding contract: VERIFYING.md.

## Frozen design

- Bundles: tools grouped by namespace in owner config (e.g. `browser_*`, `tavily_*`, `goal_*`,
  pins, whatsapp). The extension ships NO bundle opinions; config-only. Always-on core
  (read, write, edit, bash, grep, find, ls) is hardcoded and never routed.
- Scoring: one BATCHED Jev request per epoch (descriptions only, small question payloads),
  one Noul per configured namespace over the same digest built for Nozzle 1 (share the digest
  and the epoch boundary; do not build a second pipeline).
- Surfacing: namespace schemas included in the tool set only while active. Same frozen-epoch
  rule as skills. If tool-set mutation requires removing tools mid-epoch: forbidden; apply at
  boundary only.
- Degradation: Jev unreachable / key missing / repeated 429 → fail-static (all namespaces
  visible, exactly Pi default), one `ctx.ui.notify`, `ROUTE_DEGRADED: reason=…` log line.
- Escape hatch: if the model produces a tool name that was surfaced off, the extension detects
  the unknown-tool error and surfaces that namespace at the next boundary (miss-recovery path),
  logging `TOOL_SURFACE_MISS: namespace=…`.
- Telemetry: `TOOL_SURFACE: epoch=… active=[…] scores={…}` per epoch.

## Milestones

- M1: config schema (namespaces, core list, thresholds), batched scorer over shared digest,
  boundary-applied surfacing, unit tests with fixture Jev server.
- M2: fail-static paths, unknown-tool miss recovery, telemetry, VERIFYING §5 Nozzle-2 scenarios
  as passing tests.

## Learnings

### M1 (verified 2026-09-18)

- Digest sharing: wiring builds the digest once per `before_agent_start` and passes it to both
  routers (`digest?` on the skill router input); routers run in parallel via `Promise.all`.
- Tool-set seams (`pi.getAllTools/getActiveTools/setActiveTools`) are injected as `deps.tools`;
  when absent the tool router is not constructed and Nozzle 1 behavior is byte-identical.
- Core protection is literal: `coreTools` (hardcoded floor ∪ owner config) is force-kept even
  when an owner namespace mistakenly lists a core tool.
- `setActiveTools` is called only on actual set change (no-op suppression keeps prefix caches
  and user edits to unconfigured tools stable).
- Fail-static mechanism landed in M1 (restore-all + ROUTE_DEGRADED nozzle=tools); M2 owns the
  §5 degradation scenario mirror, miss recovery, and the TOOL_SURFACE telemetry JSONL record.

## Acceptance

`npm run check && npm test` green; Nozzle-2 scenarios mirrored as tests; shares digest/epoch
machinery with Nozzle 1 (no duplicated pipeline code); zero runtime deps.
