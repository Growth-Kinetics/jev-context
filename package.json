{
  "name": "jev-context",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Jev-routed context governor for Pi: skill routing, tool surfacing, epoch pruning. Verification contract: VERIFYING.md.",
  "scripts": {
    "check": "biome check --error-on-warnings . && tsgo --noEmit -p tsconfig.json && node --experimental-strip-types scripts/check-pinned-deps.mts",
    "test": "node --test --experimental-strip-types $(find . -name '*.test.ts' -not -path './node_modules/*')"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "@earendil-works/pi-agent-core": "0.85.1",
    "@earendil-works/pi-ai": "0.85.1",
    "@earendil-works/pi-coding-agent": "0.85.1",
    "@types/node": "26.6.1",
    "@typescript/native-preview": "7.0.0-dev.20260707.2"
  }
}
